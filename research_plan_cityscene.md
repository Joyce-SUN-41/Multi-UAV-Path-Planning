# 改进计划（第二轮）：城市配送场景高度仿真精致化

## 用户反馈（2026-08-08 第九轮后）
三套难度仍"丑陋/简单/粗糙/路径糊"，具体要求：
1. 不同楼的形状大小不应该一样 → 楼宇形态必须差异化、精致。
2. 不像完备城市环境 → 太粗糙，需要城市级结构（路网/功能区/地标/绿地/水体）。
3. 路径曲线太不清晰太糊 → 轨迹渲染重做，白底优先清晰。
最终目标：三个复杂度场景从**布局→形状→颜色**都高度仿真真实城市。

## 设计原则（硬约束）
- 不改动 CA 算法任何内部逻辑（接口调用形式不变）。
- 只扩展障碍"类型/布局生成器/渲染"与轨迹渲染；代价函数签名 `cost=fhd(x,scene)` 不变。
- 白底工程风保持（长期记忆）；RandStream(seed) 可复现。
- 碰撞检测 mu_obstacle_dist 与渲染 mu_draw_scene 对新类型必须同步。

## 核心数据结构决策
抛弃 `row`（整排统一块）。改为**单栋 rich-building 为原子对象**：
- 每栋楼字段：`type='bldg'`, `c`(基座中心 xy), `foot`(半宽 [hwx hwy] 或多边形 Nx2),
  `baseH`(裙楼高), `bodyH`(塔身高), `setback`(退台收进比例), `topH`(总高),
  `kind`('residential'|'office'|'landmark'|'civic'), `hue`(明度抖动)。
- 碰撞：分两段盒体（裙楼 + 退台塔身）取最近有符号距离；多边形 foot 用点到多边形距离。
- 渲染：基座块 + 退台块 + 楼顶设备小盒，逐面明暗 + 窗格。

## 改进 1：楼宇形态差异化（形状）
- 平面形状：矩形 hw 独立随机（比例 0.6~1.6），可选 L 形（切角多边形）。
- 竖向构成：裙楼 baseH(2~4 层, 更宽) + 塔身 bodyH(收分) + 顶部退台(收进 setback 0.1~0.25)。
- 高度混合分布：低层 12~45m(60%)、中层 45~90m(30%)、高层地标 90~140m(10%，仅 CBD/landmark)。
- 楼顶：设备机房小盒 + 天线细杆（随机），增强真实细节。
- 每栋独立 hue 抖动 ±0.06，避免死板。

## 改进 2：完备城市结构（布局）
在 mu_city_layout 引入城市级非均匀布局（泊松盘/聚类采样，非规则阵列）：
- 路网：主干道（十字或环形轴线，宽 35~50）+ 次干道（网格，宽 16~24），楼宇沿道路贴线布置留出街道峡谷。
- 功能区（按难度分配面积比例）：
  - 居住区 residential：中低密度板楼群（成簇）。
  - CBD office：少量超高地标簇（>100m）+ 中层塔。
  - 绿地 park：成片树木 + 开阔无楼区（仅参与起终点约束）。
  - 水体 water：沿河/湖，地形下凹 + 沿边禁飞/限飞，无楼。
  - 工业 industrial：储罐/厂房 + 塔架。
- 地标：每城 1~3 栋 >100m 焦点塔。
- 边界层次：城市外围绿化带 + 机场禁飞区，形成"城—郊—野"。
- 难度映射：
  - easy（新城/郊区）：路网稀疏、楼矮而疏、大片绿地、1 禁飞、地形缓。楼数 ~40。
  - medium（典型城区）：完整路网 + 居住/CBD/公园混合、中高层、2~3 禁飞、中等地形。楼数 ~70。
  - hard（密集老城）：路网密、楼高而密、CBD 地标簇 + 狭窄街道峡谷、多禁飞 + 水体、明显地形。楼数 ~120。

## 改进 3：颜色高度仿真（颜色）
- 楼宇按 kind + 高度分层着色：
  - residential 暖灰 [0.62 0.60 0.58]，office 冷灰蓝 [0.55 0.62 0.72]，
    landmark 深蓝灰 [0.40 0.48 0.62]，civic 浅米 [0.70 0.68 0.60]。
  - 随高度做轻微亮度衰减（越高略浅，模拟大气/玻璃反光）。
  - 窗格：按朝向用两种明度交错（模拟玻璃幕墙反光），夜间感可选暖黄点窗。
- 地形：按高度伪彩色（低绿→高棕）或浅灰等高线。
- 禁飞区：红橙半透明 + 民航斜纹感描边。
- 水体：浅蓝半透明面。
- 轨迹配色：高饱和区分色（见改进 4）。

## 改进 4：轨迹清晰化（路径糊）
重做 mu_traj_gradient 白底策略（清晰优先于发光）：
- 主体 = **等宽实线中心线**（LineWidth 2.8~3.5，不透明深饱和色），取消半透明 ribbon 面。
- 仅首尾轻微淡出渐变（fade 0.15），核心连续清晰。
- 轨迹采样密度 smooth 60→140，曲线更顺滑。
- 每条轨迹加白色细描边（LineWidth+1.2, 白）防交叠糊团。
- 起点→终点方向箭头标记（可选）。

## 分阶段执行计划
- **阶段 1（形状）**：定义 rich-building 数据结构 + mu_obstacle_dist 的 'bldg' 分支（两段盒/多边形距离）+ mu_draw_scene 的 drawBuildingRich（基座+退台+楼顶+窗格）。先用简单网格生成器验证单栋渲染/碰撞。
- **阶段 2（布局）**：重写 mu_city_layout 为城市分区 + 路网贴线 + 泊松聚类 + 绿地/水体/工业/CBD；更新 mu_depots（边界绿化带）；更新 mu_customer_points（挂 rich 楼顶）。
- **阶段 3（颜色）**：按 kind/高度分层着色 + 窗格玻璃反光 + 地形伪彩 + 水体面 + 禁飞区斜纹。
- **阶段 4（轨迹）**：重做 mu_traj_gradient 清晰化 + smooth 密度提升 + 白描边。
- **阶段 5（集成验证）**：更新 demo 预算/summary；run_demo 跑 4 场景确认零穿透；预览图人工核对"像真实城市 + 路径清晰"；更新 GUI 文案（如需）。
- **阶段 6（清理）**：删除临时验证 .m/.bat；更新 memory（daily + MEMORY.md）。

## 验证
- 4 场景零穿透（mu_obstacle_dist 诊断）。
- 预览图显示：差异化楼宇形状、城市级路网/功能区、清晰轨迹。
- 代价函数签名、CA 接口不变。
- checkcode 语法通过。

## 风险
- rich-building 多边形距离 + 退台两段距离计算量增大（hard 120 栋 × 轨迹采样），需控制 smooth 与 maxFE；row 旧类型可保留兼容或移除。
- 泊松盘采样在 MATLAB 无内置，用简单网格抖动 + 拒绝采样近似。
- 不改动 CAv9x 任何逻辑（长期记忆硬约束）。
