# 城市复杂环境模型改进方案（Multi-UAV-Path-Planning）

> 目标：将当前以"障碍避让"为核心的简化城市模型，升级为具备真实城市肌理、可支撑无人机路径规划与城市数字孪生仿真的**完整环境模型**。重点解决现有模型的"密度不合适"问题，并按需求补全道路、动态物体、通信、传感器、材质、大气、天气等要素。
>
> **参照城市：重庆（山地立体城市）**。本方案以重庆为原型注入"层次性"与"适度密集"两个核心诉求——
> - **层次性（layered city）**：地形高差（两江三岸、坡地梯田）→ 建筑依山就势退台与吊脚 → 密集高楼簇拥在谷地与半岛 → 立体交通（跨江大桥、高架、轻轨穿楼）→ 空域分层（低空街道峡谷 / 中层可飞带 / 高空走廊）。
> - **适度密集**：三个难度都需"看得到密度感"，但保留无人机可飞通道；密度通过 BCR/FAR 与街宽解耦控制，而非硬凑楼数。

---

## 1. 现状评估与密度问题诊断

### 1.1 现有模型能力（已具备）
- 建筑：`bldg`（裙楼+退台塔身、L 形 foot、楼顶设备），`tower`（通信塔架），`cube/building` 别名
- 地形：`terrain` 高度场函数句柄（easy/medium/hard 不同程度起伏）
- 障碍类：`cylinder`（储罐）、`sphere`（禁飞球/树冠）、`nofly`（多边形禁飞区）、`tree`（软植被）、`water`（水体）
- 任务点：仓库 depot + 客户点 tasks（挂在楼宇上）
- 碰撞检测：`mu_obstacle_dist` 支持全部 8 种类型的有符号距离 + 平滑穿透惩罚

### 1.2 密度问题的根因（关键弊病）
当前 `mu_city_layout` 用**单一 `roadW` 同时充当"道路宽度"和"路网网格步长"**，楼宇通过"聚类高斯抖动 + 贴到最近次干道交点"放置：

```
ngrid = round(2*half / (roadW*2))   % 次干道数
gx = linspace(-half+roadW, half-roadW, ngrid)
% 楼宇贴到最近交点 ± roadW*0.30，重叠则用 minsep 拒绝
```

这导致三个失真：
1. **easy 过散**：roadW=46 → ngrid=4（仅 4×4=16 个贴线点），40 栋楼摊在 16 点上，街区过大，像稀疏棋盘而非城市。
2. **hard 过挤且失真**：roadW=30 → ngrid=7（49 个贴线点），125 栋硬塞进 49 点附近，楼全堆在交叉口周围，街道被填满，无真实"街道峡谷"（street canyon）宽高比。
3. **难度只是数量变化**：easy/medium/hard 布局模式完全相同（粗网格贴线），没有"郊区 / 典型城区 / 老城"的肌理差异。
4. **道路宽度与楼密度耦合**：无法独立控制街宽（影响无人机低空通道）与建筑密度（影响遮挡），二者被 `roadW` 锁死。

**结论**：密度不合适的本质是"用粗网格贴线生成楼，而非基于真实地籍地块（block）与容积率生成城市"。需重构为"路网先行 → 地块划分 → 地块内按覆盖率/容积率布置建筑"的管线。

---

## 2. 改进目标与原则

- **层次性（核心新增）**：三个难度都必须是"有层次的立体城市"，而非平面铺开。层次来自四个维度——地形高差层、建筑高度分层、立体交通层（桥/高架/轻轨）、空域使用分层。难度差异体现在"层次落差大小"与"密度梯度"，而非有无层次。
- **适度密集**：三个版本建筑覆盖率（BCR）整体上抬（见 4.2），easy 也需有"成片楼群"的密集感，hard 接近真实密老城；密集但必须保证最小可飞走廊（楼间隙 ≥ 无人机安全宽 + 安全裕度）。
- **真实肌理**：路网分级（arterial/collector/local），建筑沿地块布置，形成可飞行的街道峡谷。
- **密度可控且分离**：街宽、建筑覆盖率（BCR）、容积率（FAR）作为**独立参数**，难度映射为合理城市类型而非单纯数量。
- **要素完整**：覆盖用户列出的全部 12 类要素，统一挂载在 `scene` 结构体下。
- **无人机导向**：所有静态/动态要素都提供"有符号距离场 / 运动轨迹 / 可碰撞性"接口，供 `mu_obstacle_dist` 与代价函数复用。
- **向后兼容**：保留现有 `bldg/tower/nofly/tree/water/terrain` 类型与 `mu_cost_*`/`mu_draw_scene` 接口；新增要素以扩展字段方式接入，不破坏已有规划链路。

---

## 3. 分层模型架构（对应 12 类要素）

### 3.1 环境参考系（坐标系统）【新增核心】
- **默认局部笛卡尔**：维持现有 `bounds=[-200 200 -200 200 0 150]`（米），原点为城市中心，Z 向上。
- **可选地理锚点**：`scene.geoRef = struct('datum','WGS84','originLat',..,'originLon',..,'originAlt',..,'heading',..)`，提供局部坐标↔经纬度转换 `mu_geo2local / mu_local2geo`（helmert 2D + 高程偏移）。用于与真实 GIS/OSM 数据对接。
- **尺度约定**：所有几何单位统一为米；时间单位为秒；角度单位为度（航向）或弧度（内部计算）。

### 3.2 地面 / 地形（平面或地形）【升级】
- 现有 `terrainF` 保留（地形高度场）。新增 `scene.ground`：
  - `ground.type`：`'flat' | 'terrain'`
  - `ground.surface`：路面材质索引（沥青/草地/水面），供反射率与动态物体运动基使用
  - `ground.demRes`：DEM 采样分辨率（用于激光雷达仿真）
- 地形与道路解耦：道路铺设在地形之上（道路中线有自身高程 `z_profile`），建筑基底贴合地形。

### 3.3 道路网络（中心线 / 宽度 / 交叉口）【新增核心，解决密度】
- **分级路网**：`scene.roads` 为结构体数组，每条路含：
  - `id`, `class`（`'arterial'|'collector'|'local'`）
  - `centerline`：N×2 折线（米），可曲线
  - `width`：车道总宽（arterial 30–40m / collector 18–24m / local 8–12m）
  - `lanes`, `sidewalk`
- **交叉口**：`scene.junctions` = 节点数组（位置 + 连接道路 id + 信号相位），供动态车辆/行人生成路径。
- **重庆式立体路网（关键层次要素）**：路网不只在平面，而带 `z_profile`（沿中心线的地面高程）以贴合地形；并显式生成两类立体交通结构：
  - `bridge`（跨江/跨谷大桥）：连接被 water/河谷分隔的地块，有独立桥面高程，作为可飞越结构，也是 L2 立体交通层的主体。
  - `viaduct`（高架快速路）：沿地形抬升的 `local`/`collector` 段，渲染为悬空路面板，下方仍可通行低空无人机。
  - 轻轨穿楼廊道（`bridge` 型窄廊）穿插于高层带楼宇之间，作为动态物体（列车）轨道。
- **生成管线**：先用 L 系统 / 曼哈顿网格 + 随机扰动生成分级路网（带 z_profile）→ 路网围成**地块（block）** → 建筑在地块内布置（见 3.4）。这一步直接解决"密度不合适"：街宽由道路 class 决定，建筑密度由地块覆盖率决定，二者解耦；立体交通结构注入 L2 层次。

### 3.4 建筑（位置 / 尺寸 / 高度 / 外观）【升级 + 密度控制 + 重庆层次】
- 保留 `bldg`（裙楼+退台+L 形+楼顶设备）。扩展字段：
  - `bldg.use`：`'residential'|'office'|'landmark'|'civic'|'industrial'`（影响外观色与材质）
  - `bldg.materialId`：指向 3.9 材质表
  - `bldg.coverage`：地块内覆盖率（用于密度校验）
  - `bldg.tier`：高度分层标签（见 3.4.1），用于渲染分层色与空域分层判断
- **层次化高度分布（重庆式）**：每栋楼依地块标高（地形层）与所属高度层生成，形成"低层裙房连片 + 中层塔楼簇 + 高层地标塔拔地"的竖向层次。密度通过 BCR 控制，高度通过分层比例控制，二者解耦。
  - 四层高度带（相对地块地坪）：低层带 L（≤30m，裙房/住宅）、中层带 M（30–80m，塔楼）、高层带 H（80–140m，高层簇）、地标带 T（140–220m，少量 CBD 焦点塔）
  - 难度决定四层占比与地块地坪高差（见 4.2）
- **密度控制（核心修复 + 适度密集）**：
  - 引入目标 **建筑覆盖率 BCR** 与 **容积率 FAR** 作为难度参数，而非固定 `nBldg`：
    - easy（半岛新城）：BCR≈0.28、低中层为主、少量高层（hLo 15 / hMid 60 / hHi 110）
    - medium（坡地主城）：BCR≈0.42、中高混合、明显高差（hLo 15 / hMid 90 / hHi 160）
    - hard（立体老城）：BCR≈0.55、高层簇 + 窄街 + 陡坡深谷（local 路宽 8m，hLo 18 / hMid 110 / hHi 200）
  - 建筑在地块内按"地块面积×BCR / 单栋 footprint"估算数量，而非全局硬凑 `nBldg`
  - 街宽高比（aspect ratio H/W）约束：hard 老城窄街（W=8, H=200 → H/W≈25，强烈峡谷感）；easy 宽街（W=28, H=110 → H/W≈4，开阔但仍成片）

### 3.4.1 层次性设计专章（重庆参照，四个层次）

本方案把"层次性"作为三个难度的共同骨架，差异只在落差与密度梯度：

- **L0 地形层（Geography）**：用重庆式"两江三岸 + 坡地梯田"高度场。同一 difficulty 内地形高差明显（谷地低、台地高、江岸陡），建筑基底贴合地形 → 楼群自然呈现"沿等高线退台、高低错落"的竖向层次，而非齐平地坪。
- **L1 建筑高度层（Vertical tiers）**：每个难度按 3.4 的四带（L/M/H/T）分布，低层裙房连片成底、中层塔楼成片、高层簇拥成峰、地标塔零星拔起。渲染时对四带用微差色阶，强化层次可读性。
- **L2 立体交通层（3D mobility）**：跨江大桥（`bridge`，box+栏杆，自身高程跨谷/跨江）、高架快速路（沿地形抬升的 `local` 路，带 `z_profile`）、轻轨穿楼（在高层带楼宇中穿插一段 `bridge` 型廊道，作为可飞越结构 + 动态物体轨道）。这让"路不在地面、楼中有轨"的重庆意象可仿真。
- **L3 空域使用层（Airspace stratification）**：为无人机规划显式分层——低空街道峡谷带（z∈[ground+15, ground+60]，贴楼可飞）、中层可飞带（z∈[60, 100]，跨楼间）、高空走廊（z∈[100, 150]，越过高层簇）。代价函数可按当前 `z` 给层级偏好（如低空给轻微引导、高层簇顶需绕飞），GUI 可按层半透明显示。

> 层次性 ≠ 单纯堆高。关键是**相邻层之间有可识别的过渡与可飞间隙**：地形高差给底面层次，高度带给竖面层次，桥/轨给立体交通层次，空域层给规划层次。三个难度都具备这四层，只是 hard 的落差与密度更大。

### 3.5 基础设施（树木 / 路灯 / 桥梁 / 交通标志）【补全】
- `tree`（已有，软障碍）保留并扩展 `materialId`（植被介电常数）。
- 新增：
  - `streetlight`：沿 local 路中线按间距布置，含杆高/灯具，作为低空软障碍 + 夜间光源
  - `bridge`：跨越 water/河谷的桥面（box + 栏杆），有自身高程，作为可飞越结构
  - `trafficsign`：交叉口附近的标志牌（小 box），视觉 + 低空障碍
- 所有基础设施复用 `mu_obstacle_dist` 的类型分支（新增 `streetlight/bridge/sign` 类型）。

### 3.6 动态物体（车辆 / 无人机 / 行人及轨迹）【新增核心】
- `scene.dynamics`：结构体数组，每项含：
  - `type`：`'vehicle'|'pedestrian'|'uav'|'other'`
  - `path`：参数化轨迹（沿道路中心线的弧长参数 + 速度剖面），或关键帧
  - `motion`：运动模型（`'const'|'ctrv'|'orbital'`），供时变碰撞检测 `mu_obstacle_dist_t(traj, t)`
  - `size`：包围盒（用于碰撞）
  - `schedule`：出现时间窗 `[t0,t1]`（部分时段存在）
- **与路径规划的关系**：动态物体默认作为**时变软约束**（代价函数可按当前规划时刻评估），GUI/回放可按时间轴播放（已有 `playAnim` 可扩展为含动态物体）。
- 车辆/行人轨迹由 3.3 路网生成（沿 centerline + 交叉口转向），保证"在道路上运动"而非空中乱飞。

### 3.7 通信设备（基站 / 用户终端 / 天线参数）【新增】
- `scene.comms`：结构体数组，每项：
  - `type`：`'base_station'|'terminal'|'relay'`
  - `pos`：位置（基站可在楼顶/独立塔）
  - `antenna`：`struct('freqGHz',, 'gaindBi',, 'beamwidth',, 'polarization',, 'tilt',, 'txPower')`
  - `coverage`：预估覆盖半径（用于通信约束，可选代价项 `w.comms`）
- 用途：无人机通信链路约束（路径需保持在基站覆盖内），或作为可视化参考。

### 3.8 传感器（激光雷达 / 摄像头 / GPS 等）【新增】
- `scene.sensors`：挂载在无人机或固定点，每项：
  - `type`：`'lidar'|'camera'|'gps'|'imu'|'radar'`
  - `mount`：挂载平台（无人机 id 或固定点）
  - `params`：量程/视场角/分辨率/噪声模型（供后续感知仿真）
- 当前阶段以"元数据 + 可视化"为主，不强制接入规划链路；预留 `mu_sensor_footprint` 接口供未来感知约束。

### 3.9 材质属性（介电常数 / 反射率等）【新增】
- `scene.materials`：材质查找表，每项：
  - `id`, `name`
  - `permittivity`（相对介电常数 εr）、`conductivity`（σ）
  - `reflectivity`（可见光/雷达）、`roughness`
  - `colorRGB`（外观）
- 建筑/地面/基础设施通过 `materialId` 引用。为通信（3.7）、雷达反射（感知）、外观渲染提供统一数据底座。

### 3.10 大气条件（温度 / 湿度 / 气压）【新增】
- `scene.atmosphere`：`struct('tempC',, 'humidity',, 'pressure',, 'wind',[vx vy vz], 'visibility')`
- 影响：无人机气动/能耗模型（可选）、通信传播衰减、天气效果（3.11）的驱动。

### 3.11 环境动态变化（光照 / 天气效果）【新增】
- `scene.envdyn`：
  - `timeOfDay`：驱动光照方向/强度（`mu_draw_scene` 据此设太阳方向阴影）
  - `weather`：`'clear'|'cloudy'|'rain'|'fog'|'snow'`，影响 `visibility`、`reflectivity`、动态物体速度（雨天降速）
  - `lighting`：太阳方位/天空盒参数，供渲染
- 与 3.6 动态物体、3.10 大气联动：例如 fog 降低 visibility → 动态物体出现距离缩短。

---

## 4. 密度修正专项（核心交付，重庆式层次城市）

### 4.1 重构布局管线（替代现有贴线法）
```
mu_city_layout(difficulty):
  1. 生成重庆式地形高度场 (谷地/台地/江岸陡坎) + 水体(江/河)   <- 新增 L0
  2. 生成分级立体路网 (arterial/collector/local, 带 z_profile)   <- 新增 L2 底
  3. 路网做地块划分 (polygon boolean, 贴合地形)                  <- 新增
  4. for each block:
       目标栋数 = floor(blockArea * BCR / avgFootprint)
       在地块内用 Poisson-disk / 网格抖动放置，满足 minsep
       每栋按 L/M/H/T 四带比例抽高度层 (tier)                  <- 新增 L1
  5. 跨江/跨谷布置 bridge；抬升段布置 viaduct；高层带穿插轻轨廊道  <- L2
  6. 沿 local 路布置 streetlight/sign；公园/街角布置 tree
  7. 输出空域分层参数 (L3: 低空/中层/高空走廊边界)
```
- **街宽与密度解耦**：`roadW` 拆分为 `arterialW/collectorW/localW`，建筑密度由 `BCR` 控制（二者独立）。
- **适度密集 + 层次**：三个难度均按 4.2 的 BCR 上抬，easy 也成片；高度四带 + 地形高差提供竖向层次，避免 easy 像"稀疏棋盘"。

### 4.2 难度映射表（重庆式三梯度，密度整体上调）
| 参数 | easy（半岛新城） | medium（坡地主城） | hard（立体老城） |
|---|---|---|---|
| 城市意象 | 平缓半岛、成片新楼、少量高层 | 明显坡地、中高混合、簇群 | 陡坡深谷、超密高层簇、跨江桥+轻轨 |
| BCR（建筑覆盖率） | 0.28 | 0.42 | 0.55 |
| FAR（容积率） | 1.5 | 3.5 | 6.0 |
| 地形高差 (m) | 8–15（缓坡） | 20–40（坡地梯田） | 40–80（陡坡深谷） |
| 路网 | arterial+完整local | 完整三级 | 密三级+窄街+高架 |
| local 路宽 (m) | 28 | 16 | 8 |
| 高度四带占比 L/M/H/T | 50/35/13/2 | 35/35/25/5 | 20/30/35/15 |
| 地标塔高 H_typ (m) | 110 | 160 | 200 |
| 立体交通 | 1–2 跨江桥 | 桥+部分高架 | 桥+高架+轻轨穿楼 |
| 动态物体密度 | 低 | 中 | 高 |
| 天气/光照 | 晴昼 | 多变(多云/晴) | 雾/黄昏/雨 |

> 密度"稍微密集一点"的具体落地：相对旧方案（0.20/0.35/0.45），新 BCR 为 0.28/0.42/0.55，整体 +0.07~0.10；同时取消"楼堆交叉口"的贴线法，改为地块内 Poisson-disk 均匀密铺，使密集感均匀而非局部拥堵。最低可飞走廊约束（4.3）保证密集下仍有解。

### 4.3 校验指标
- `BCR` 实际值 ≈ 目标（误差 <5%）
- 平均街宽高比 H/W 在合理范围（easy≈4、medium≈8、hard≈25，呈现层次落差）
- 楼宇间最小间隙 ≥ 无人机安全走廊宽度（如 6m），保证可飞通道存在——密集但**必须**有解
- 地形高差、桥/轨立体结构已生成（层次性达标）
- 起点/终点/任务点不被建筑覆盖（现有 `mu_assign_depots`/`mu_customer_points` 已做拒绝采样，保留）

---

## 5. 数据结构设计（scene 扩展）

```matlab
scene = struct( ...
  'mode', [], 'difficulty', [], 'nUAV', [], 'nCtrl', [], ...
  'bounds', [], 'starts', [], 'goals', [], ...
  'obstacles', [],        % 现有 bldg/tower/nofly/tree/water/terrain + 新增 streetlight/bridge/sign
  'terrainF', [], 'ground', struct(), ...
  'roads', [], 'junctions', [], ...        % 新增 3.3
  'tasks', [], 'taskAssign', [], 'depots', [], ...
  'dynamics', [], ...                       % 新增 3.6
  'comms', [], ...                          % 新增 3.7
  'sensors', [], ...                        % 新增 3.8
  'materials', [], ...                      % 新增 3.9
  'atmosphere', struct(), ...               % 新增 3.10
  'envdyn', struct(), ...                   % 新增 3.11
  'geoRef', struct(), ...                   % 新增 3.1
  'w', struct(), 'smooth', [], 'seed', [] );
```

向后兼容：`mu_obstacle_dist` / `mu_cost_*` / `mu_draw_scene` 仅读取既有字段；新增要素通过"可选字段 + 类型分支"接入，旧场景（不含新字段）仍正常运行。

---

## 6. 实施路线（分阶段，建议顺序）

- **阶段 A（密度修复 + 层次化，优先）**：重构 `mu_city_layout` 为"重庆式地形→立体路网→地块→按 BCR 密铺 + 四带高度分层"。引入 `roads/junctions` 与地形高差；easy/medium/hard 映射为半岛新城/坡地主城/立体老城（见 4.2，BCR 0.28/0.42/0.55）。建筑带 `tier` 标签，渲染分层色，并产出 L3 空域分层参数。验证 BCR/街宽高比/最小可飞走廊达标，且零穿透仍可解。
- **阶段 B（静态要素补全）**：加入 `streetlight/bridge/sign` 类型到 `mu_obstacle_dist` + `mu_draw_scene`；补全 `materials` 表并接入建筑/地面。
- **阶段 C（参考系与大气/环境动态）**：`geoRef` 转换函数；`atmosphere` + `envdyn` 接入渲染（光照/天气）与可选通信/感知约束。
- **阶段 D（动态物体）**：`dynamics` 生成（沿路网轨迹）+ 时变碰撞检测 `mu_obstacle_dist_t` + GUI 时间轴回放。
- **阶段 E（通信/传感器）**：`comms`/`sensors` 元数据 + 可视化；可选 `w.comms` 链路约束。

每阶段保留 `verify_phase*.m` 式回归验证，确保不破坏已有零穿透规划链路。

---

## 7. 风险与验证

- **性能**：新增路网/动态物体/材质会增加 `mu_obstacle_dist` 分支与场景内存；对 hard 高密度场景需控制障碍总数（建议 ≤300 个实体）。
- **规划可解性**：密度升高（hard BCR=0.45）需相应提高 CA 的 `maxFE` 与 `pop`，已在 `run_demo` 参数中预留。
- **验证手段**：每个阶段用轻量规划（easy 低参数）确认零穿透仍可达；`verify_fix` 扩展断言新增类型的有符号距离正确性；GUI 人工核对"像真实城市 + 路径清晰 + 街道峡谷可见"。
- **向后兼容测试**：旧 `results/` 重跑对照，确认新模型下代价/穿透指标合理。

---

## 8. 假设与待确认项

- **坐标系**：默认局部笛卡尔（米），可选经纬度锚点——是否需对接真实 OSM/GIS 数据？
- **精度级别**：是概念级仿真（外观+碰撞）还是需电磁波级材质（如射线追踪）？影响 3.9/3.7 深度。
- **动态物体是否进入规划代价**：默认作为可视化/回放，还是作为时变硬约束？影响 3.6 与 `mu_cost_*` 接口。
- **天气/光照是否影响规划**：默认仅影响渲染与感知，还是也影响无人机动力学/通信？

> 以上假设可在方案确认后细化；阶段 A（密度修复）可立即启动，不依赖这些决定。
